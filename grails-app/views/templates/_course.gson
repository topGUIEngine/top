import edu.oswego.cs.lakerpolling.domains.Course

model {
    Course course
}

json {
    name course.name
    crn course.crn
    students course.students?.size()
    instructor g.render(template: '../templates/user', model: [user : course.instructor])
}