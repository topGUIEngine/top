import edu.oswego.cs.lakerpolling.domains.User

model {
    User student
}

json {
    first student.firstName
    last student.lastName
    name String.format("%s %s", student.firstName, student.lastName)
    email student.email
}